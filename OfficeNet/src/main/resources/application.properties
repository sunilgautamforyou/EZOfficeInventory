spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp
server.servlet-path=/*
spring.servlet.multipart.enabled=true
spring.resources.static-locations=classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/dsndb?autoReconnect=true&useSSL=false
# Prod Password
spring.datasource.username=salepur7_ahsjkomcqm
spring.datasource.password=India@600701
#Local System Password
#spring.datasource.username=root
#spring.datasource.password=root
spring.datasource.platform=mysql
server.servlet.session.timeout=1800
logging.level.com.tradestrome.loanApp.SqlMapper=TRACE
logging.level.org.springframework = info
logging.level.tech.hardik = trace
logging.pattern.file= "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# To store logs to external file
# Here use strictly forward "/" slash for both Windows, Linux or any other os, otherwise, its won't work.      
#logging.file=D:/spring_app_log_file.log

# To customize logging pattern.
logging.pattern.file= "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

#server.error.whitelabel.enabled=false
server.error.path=/error


## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size